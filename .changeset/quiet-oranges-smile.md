---
"effect-attio": patch
---

Only include standard-object record references when their target object is enabled. Add `Attributes.RecordReference.For(object)` for target-specific custom object references.
